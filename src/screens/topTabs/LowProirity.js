import React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const LowPriority = props => {
  return (
    <View>
      <Text>This are Low priority pointers</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default LowPriority;
